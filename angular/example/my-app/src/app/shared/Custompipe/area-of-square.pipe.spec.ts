import { AreaOfSquarePipe } from './area-of-square.pipe';

describe('AreaOfSquarePipe', () => {
  it('create an instance', () => {
    const pipe = new AreaOfSquarePipe();
    expect(pipe).toBeTruthy();
  });
});
